<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label, "Miejsce w serwisie" %><br />
    <%= f.select :label, options_for_select(Page::PAGE_LABELS) %>
  </div>
  <div class="field">
    <%= f.label :content, "Treść" %><br />
    <%#= f.text_area :content %>
    <%= f.tinymce :content %>
  </div>
  <div class="actions">
    <%= f.submit "Zatwierdź" %>
  </div>
<% end %>
